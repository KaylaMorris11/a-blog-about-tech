{{#each posts as |post| }}
<div>
    <h1>
        <a href="/api/posts/{{post.id}}">
        {{post.title}}
        </a>
    </h1>
    <p>
        Posted by {{post.user.username}} on {{format_date post.createdAt}}
        <a href="/api/posts/{{post.id}}"></a>
    </p>
    <div>
        
</div>
    {{/each}}